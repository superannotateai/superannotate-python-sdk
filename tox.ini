[tox]
isolated_build = True

envlist = py38

[testenv]
usedevelop = true
deps =
    -rrequirements.txt

[testenv:py37]
deps =
    {[testenv]deps}
commands =
    pytest -q {posargs}

[testenv:py38]
deps =
    {[testenv]deps}
commands =
    pytest -q {posargs}

[testenv:linter]
deps =
    flake8
skip_install =
    true
basepython =
    python3
commands =
    flake8 --max-line-length=120
exclude =
    src/lib/app/analytics,lib/app/converters,lib/app/input_convertors


[testenv:mypy]
deps=
    mypy
    -rrequirements.txt
skip_install =
    true
basepython =
    python3
commands=
    mypy lib --ignore-missing-imports
exclude =
    src/lib/app/analytics,lib/app/converters,lib/app/input_convertors

[testenv:coverage]
deps =
    {[testenv]deps}
    pytest-cov==2.8.1
basepython =
    python3
commands =
    pytest -q --cov-report term --cov='src/superannotate' --cov-fail-under=80 {posargs}

[testenv:pre-commit]
deps =
    pre-commit
skip_install =
    true
basepython =
    python3
commands =
    pre-commit run --all-files
